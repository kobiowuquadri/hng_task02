// jest.setup.js

// Example: Setting up a custom environment variable
process.env.NODE_ENV = 'test';

// You can add other global setup configurations here

// If you need to mock real modules, add them here.
// Example for a real module:
// jest.mock('real-module', () => {
//   return {
//     realFunction: jest.fn(),
//   };
// });
